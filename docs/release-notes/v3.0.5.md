# v3.0.5 - Preview and metadata alignment

Author: Neil Mitchell
Last modified by: Neil Mitchell

The homepage share card now matches the current “Complex work. Clear direction.” photographic
portfolio. Works has a distinct collection card, and a separate image is ready for GitHub's
repository social-preview setting. The existing project artwork and career/résumé facts are preserved.

Open Graph, X, canonical URLs, structured data, install metadata, README imagery, and operating
documentation are audited together. New image URLs distinguish the refreshed website cards from
the older cached artwork. The previous root image path remains a copy of the current homepage card.

The preview generator renders the local static export's actual font, colours, portrait, and copy.
Generated images identify Neil Mitchell as creator and modifier. Export checks cover all three
public routes, actual PNG dimensions, social alternative text, schemas, crawler files, and the
original case-study image hash.

See [the audit record](../brand-preview-audit.md) for verification and publication status.
Deployment, GitHub image upload, wiki publication, and third-party preview refresh are separate
checks; a source change alone does not prove those have occurred.
