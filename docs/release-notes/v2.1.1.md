# Neil Mitchell Portfolio v2.1.1

Version 2.1.1 repairs Vercel branch previews while preserving the production metadata and visual
experience introduced in version 2.1.0.

## Visitor-facing changes

- No visual, content, résumé, navigation, or responsive-layout changes.
- Social previews continue to use the production image at
  `https://neilmitchell.ca/opengraph-image.png`.

## Maintainer changes

- Emits the Open Graph and X image as an explicit absolute production URL in every environment.
- Keeps the strict production-domain metadata validation intact.
- Restores Vercel Preview as a useful validation gate for short-lived branches.

## Verification

- Formatting, ESLint, TypeScript, content validation, and the static production build pass.
- A Vercel Preview deployment completes successfully before the change is landed on `main`.
- GitHub Actions and the Vercel production deployment pass on the released commit.
- Production metadata and the public Open Graph image are checked after deployment.

## Rollback

Version `v2.1.0` remains the previous known-good release and Vercel deployment.
